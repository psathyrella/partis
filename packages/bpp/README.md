This is the version of bpp for old-style (2014-2019ish) partis simulation, i.e. before --per-base-mutation.
I think it was added using git subtree, but at some point I switched to using git submodules, so now it just looks like I copied the files.


